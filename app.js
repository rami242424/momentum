// string
// const amIFat = "true";

// boolean
const amIFat = true;
console.log(amIFat);

// null 그 변수에 아무것도 없다는 뜻(값이 없다)

//undefined : 컴퓨터 메모리 안에는 존재, 공간은 있으나 값이 들어가지 않은 것
let something;
console.log(something);